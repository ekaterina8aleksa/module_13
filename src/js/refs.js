const imageList = document.querySelector('.gallery');
const searchForm = document.querySelector('#search-form');
const btnSearch = document.querySelector('#js-loadmore');
const btnLabel = document.querySelector('.label');
const btnSpinner = document.querySelector('.spinner-grow');

export { imageList, searchForm, btnSearch, btnLabel, btnSpinner };
